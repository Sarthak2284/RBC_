<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout - Royal Bike Club</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            padding: 20px;
        }
        .checkout-container {
            max-width: 600px;
            margin: 50px auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        .checkout-container h1 {
            font-size: 28px;
        }
    </style>
</head>
<body>
    <div class="checkout-container">
        <h1 class="text-center mb-4">Checkout</h1>
        <div class="mb-3">
            <h5>User Name:</h5>
            <p><%= userName %></p>
        </div>
        <div class="mb-3">
            <h5>Bike Details:</h5>
            <p><strong>Name:</strong> <%= bike.name %></p>
            <p><strong>Category:</strong> <%= bike.category %></p>
            <p><strong>Price per Day:</strong> ₹<%= bike.rentalPrice %></p>
        </div>
        <div class="mb-3">
            <h5>Number of Days:</h5>
            <p><%= numberOfDays %></p>
        </div>
        <div class="mb-3">
            <h5>Total Payment:</h5>
            <p>₹<%= totalPayment %></p>
        </div>
        <form action="/payment" method="POST">
            <input type="hidden" name="userName" value="<%= user.firstName %>">
            <input type="hidden" name="bikeId" value="<%= bike._id %>">
            <button type="submit" class="btn btn-primary btn-block">Proceed to Payment</button>
        </form>
        
        
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
