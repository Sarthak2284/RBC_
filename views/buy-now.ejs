<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy Bike</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <div class="bg-white p-6 rounded-lg shadow-lg max-w-md mx-auto">
            <img src="<%= bike.image %>" alt="<%= bike.name %>" class="w-full h-64 object-cover rounded-t-lg mb-4">
            <h1 class="text-2xl font-semibold mb-2"><%= bike.name %></h1>
            <p class="text-gray-700 mb-2"><%= bike.description %></p>
            <p class="text-gray-800 font-bold mb-2">Price: ₹<%= bike.price %></p>
            <a href="/payment?bikeId=<%= bike._id %>" class="block bg-blue-500 text-white text-center py-2 rounded-lg hover:bg-blue-600">Proceed to Pay</a>

        </div>
    </div>
</body>
</html>
